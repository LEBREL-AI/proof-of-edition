"""Proof of Edition: verify that an inference API serves the model edition it claims.

Subpackages: receipts (signed manifests and receipts), registry (content-addressed records and the transparency log),
manifest (building serving manifests), audit (re-execution audits), fingerprint (Chain & Hash statistics and verifier)
and watch (continuous verification of the labs' APIs and the board).
"""

__version__ = "0.2.0"
